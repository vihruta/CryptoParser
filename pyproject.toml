[project]
name = "rates_service"
version = "0.1.0"
dependencies = [
    "aiohttp",
    "loguru",
    "pydantic-settings",
    "aiofiles",
    "pytest-asyncio"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 88